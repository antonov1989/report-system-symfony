# Classic session-based CSRF protection.
# (The stateless/cookie variant requires the Stimulus csrf-protection JS controller
#  to swap a placeholder token at submit time; we use server-rendered tokens instead,
#  so the dashboard login works without relying on that JS.)
framework:
    csrf_protection:
        enabled: true
    form:
        csrf_protection:
            enabled: true
